import {StyleSheet} from 'react-native';

// styles

const styles = StyleSheet.create({
  logo: {
    height: 120,
    width: 120,
    borderRadius: 60,
    alignSelf: 'center',
    marginBottom: 16,
    marginTop: 16,
  },
  image: {
    marginVertical: 16,
    height: 120,
    width: 120,
    borderRadius: 999,
  },
});

export default styles;
